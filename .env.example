# FastAPI Backend
PORT=8000
ENVIRONMENT=local

# Database (PostgreSQL + PostGIS + pgvector)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=dummy_local_password
DB_NAME=pakimongo_local

# Firebase Auth (dummy for local)
FIREBASE_PROJECT_ID=dummy-project-id

# Graphify Output

Future generated code-graph artifacts can be written here.

Keep generated files out of source-of-truth decisions. Use them as navigation and impact-analysis aids, then update `docs/` and `knowledge/okf/` when the graph reveals real architectural changes.
